# There's a bug in the 22.04 package, if mako fails to load do:
#   `sudo aa-disable /etc/apparmor.d/fr.emersion.Mako`
# See https://github.com/emersion/mako/issues/257 for details

default-timeout=10000
font=Roboto
anchor=top-right
border-size=1

[urgency=high]
ignore-timeout=1
background-color=#ff0000
