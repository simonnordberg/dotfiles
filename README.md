# dotfiles

Fedora desktop environment setup. Modular services under `services/`, each with its own `install.sh`.

```bash
git clone https://github.com/simonnordberg/dotfiles.git ~/code/dotfiles
cd ~/code/dotfiles

# install everything
bash install.sh

# install specific services
bash install.sh services/shell services/neovim
```
